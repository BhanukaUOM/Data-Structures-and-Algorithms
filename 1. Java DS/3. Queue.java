import java.util.ArrayDeque; // better than Linkedlist 

 
Queue<Node> queue = new LinkedList<Node>(); 
Push = q.add(1); //add first
Remove = q.remove(); //remove and get first
Pop = q.pop(); 
peek() // get head, not delete 
poll() = remove and get head(First). 
insert()
 
 
 
java.util.LinkedList; 
 
ArrayList<Integer> list = new ArrayList<>();   
List.remove();…. 
 
Or

import java.util.LinkedList; 
 
add, (Queue) 
isEmpty, 
poll (DeQueue) 
