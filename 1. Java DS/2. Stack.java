import java.util.Stack; 
 
Stack<Integer> stack = new Stack<Integer>(); 
         
stack.add(index(opt), 1); //insert
empty(); 
peak(); //get last without remove
pop(); //remove and get last
push(elem); //add last
stack.search(args); 
 
 
java.util.LinkedList; 
 
List.pop();…. 
